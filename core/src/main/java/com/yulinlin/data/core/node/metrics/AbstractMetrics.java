package com.yulinlin.data.core.node.metrics;

import com.yulinlin.data.core.node.INode;
import com.yulinlin.data.core.node.MetaNode;

public abstract class AbstractMetrics extends MetaNode implements INode {


    public AbstractMetrics(Object key) {
        super(key);
    }
}
