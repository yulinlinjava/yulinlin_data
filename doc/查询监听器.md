当字段分散在不同的数据源时，你可能需要一些对象后预处理的操作。



泛型为想处理的对象

```
@Component
public class ModelUserListener implements QueryListener<SysUserEntity> {

    @Override
    public void listen(SysUserEntity list) {
        //后置增强处理
    }
}
```



