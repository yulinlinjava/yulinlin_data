/*
package com.yulinlin.elasticsearch.parse;


import com.yulinlin.data.core.node.CommandNode;
import com.yulinlin.data.core.parse.IParamsContext;
import com.yulinlin.data.core.parse.IParse;
import com.yulinlin.data.core.parse.IParseManager;
import com.yulinlin.data.core.parse.ParseResult;

public class CommandNodeParse implements IParse<CommandNode> {

    @Override
    public Object parse(CommandNode condition, IParamsContext params, IParseManager parseManager) {
        return new ParseResult(condition.getType(),condition.getExpression(),params);
    }
}
*/
